# ===== WinX555 — LocalDB mode (no database) =====
# The app stores everything in the browser's localStorage. Nothing is required here.
# When you connect a real database later, add MONGODB_URI etc.
NEXT_PUBLIC_APP_NAME=WinX555
